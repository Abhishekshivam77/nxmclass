on : push
jobs :
      first-job:
          runs-on : ubuntu-latest
          steps :   
                - run : node --version
                - run : npm --version
                - uses : actions/chechout@v3
                - run : node indes.js
